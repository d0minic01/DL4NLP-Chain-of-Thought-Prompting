# =============================================================================
# Benchmark Evaluation Configuration
# =============================================================================
# limit field can be set to an integer to limit number of examples for faster testing -> set to null to run on full dataset
benchmarks:
  gsm8k:
    task: gsm8k
    limit: 2
    prompt_set: math_word_problems

  asdiv:
    task: asdiv
    limit: 5
    prompt_set: math_word_problems

  csqa:
    task: commonsense_qa
    limit: 2
    prompt_set: csqa

# =============================================================================
# Models
# =============================================================================

models:
  defaults:
    batch_size: null

  Qwen/Qwen3-0.6B:
    type: hf
    batch_size: 8
